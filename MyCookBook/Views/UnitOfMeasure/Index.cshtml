@model IEnumerable<MyCookBook.Models.UnitOfMeasure>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Unit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnitAbreviation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Unit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UnitAbreviation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UnitOfMeasureId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UnitOfMeasureId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UnitOfMeasureId })
        </td>
    </tr>
}

</table>
